Have to add c:\python\scripts to your path variable
Need Visual C++ 9.0
PyroCrypto must be 2.5+
Run StarCluster help to create the config folder
MPICH2 plugin http://star.mit.edu/cluster/docs/latest/plugins
The next step is to fill in your keypair information. If you don’t already have a keypair you can create one from StarCluster using:
$ starcluster createkey mykey -o ~/.ssh/mykey.rsa
This will create a keypair called mykey on Amazon EC2 and save the private key to ~/.ssh/mykey.rsa. Once you have a key the next step is to fill-in your keypair info in the StarCluster config file:
You have to convert the key you made in starcluster to .ppk using the convert menu option in puttygen.


Access Key Id - AKIAJT6SHIAMYVVUKGMQ
Secret Access Key:
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
Account ID - 042104091245
MPICH2 plugin

Compile Stream
mpicc -DPARALLEL_MPI -O3 -o stream_mpi stream_mpi.c
mpicc  \-o stream_mpi stream_mpi.c ///This actually runs all 8 instances.

Execute Stream
mpiexec -host master,node001,node002,node003,node004,node005,node006,node007 ./stream_mpi > outputfile.txt 
mpirun ./stream_mpi

Compile IOR
Navigate to the folder with the make file in it. Type "make" on the command line.

Execute IOR
Navigate to the C folder
mpiexec ./IOR -b 1m -t 1m > awsIOR1.txt

Compile NPB-EP
chmod 755 -R /home/ec2-user //or whatever directory your program files are in
Navigate to the MPI/config/NAS.samples folder
Copy make.def_sun_mpich
Navigate back one directory to MPI/config
Paste make.def_sun_mpich and rename it to make.def
edit make.def and delete every instance of -fast within the file
Navigate back to MPI directory and type the following command:
make ep NPROCS=8 CLASS=A  //Replace 8 with the number of nodes in the cluster


Execute NPB-EP
mpiexec bin/ep.A.8 > awsNPB8.txt

Google Compute Engine

Create Instance with Ubuntu 16.04
Install Message Passing Interface:
sudo apt-get update
sudo apt-get install openmpi-bin
sudo apt-get install gcc
sudo apt-get install libmpich-dev
sudo apt-get install libopenmpi-dev

Install Passwordless SSH:
ssh-keygen
cp .ssh/id_rsa.pub .ssh/authorized_keys

Upload and Compile stream on all servers
ssh into each vm with every other vm to get all of the hosts saved in each other's known_hosts. Or another method.

Run Stream
mpiexec -host master,node001,node002,node003,node004,node005,node006,node007 ./stream_mpi > outputfile.txt 


Install gcloud SDK.
https://cloud.google.com/compute/docs/images/import-existing-image
Create Disk Image
gcloud compute --project "subtle-champion-140118" disks snapshot "instance-1" --zone "us-east1-d" --snapshot-names "nate-snap"

Create Disk
gcloud compute --project "subtle-champion-140118" disks create "nate-disk" --zone "us-east1-d" --source-snapshot "nate-snap" --type "pd-standard" --size "10"

Create Image:
gcloud compute --project "subtle-champion-140118" images create "nate-image" --source-disk https://www.googleapis.com/compute/v1/projects/subtle-champion-140118/zones/us-east1-d/disks/nate-disk

Create Instance template with new image
Create Instance group with 8 nodes using new instance template


Compile stream

Make sure that this is replicated across NODES!
mpicc -DPARALLEL_MPI-O3 -o stream_mpi stream_mpi.c

Execute Stream
mpirun ./stream_mpi
mpiexec -host nate-instance-group-pz9j,nate-instance-group-0h67,nate-instance-group-34n8,nate-instance-group-43bk,nate-instance-group-48v1, nate-instance-group-df7n,nate-instance-group-t396,nate-instance-group-wbfz ./stream_mpi > c1.m_n4.1.txt